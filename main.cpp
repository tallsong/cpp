#include <iostream>
#include<cmath>
#include<vector>
#include<stack>
#include <algorithm>
#include<map>
#include<string>
#include<fstream>
using namespace std;
int main()
{
	vector<int> obj;//默认值为0,默认size为0
	for (int i = 0; i < obj.size(); i++)//size()容器中实际数据个数 
	{
		cout << obj.size() << ",";
	}
	return 0;
}